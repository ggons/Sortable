(function () {
  var Sortable = (function () {
    /*
      default options 정의
      기본 함수 정의
    */

    function Sortable() {
      /*
        init 호출
      */
    }

    Sortable.prototype = {
      _init: function () {
        /*
          options 정리
          event 호출
        */
      },
      _event: function () {
        /*
          sortable 관련 이벤트 정의 (위치 변경, 자동 스크롤)
        */
      }
    }

    return Sortable;
  })();

  window.Sortable = Sortable;
})();